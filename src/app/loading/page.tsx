import LoadingServerSidebar from '@/components/loading/loading-server-sidebar';
import React from 'react';

type Props = {};

function Page({ }: Props) {
    return (
        <LoadingServerSidebar />
    );
}

export default Page;